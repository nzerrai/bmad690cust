---
scope: GenData — full architecture document
purpose: validate
altitude: initiative
updated: 2026-06-23T14:55
---

- (event) Validation complète : lint mécanique (9 faux positifs), rubric walker, adversarial boundary (4 critiques, 2 hauts, 1 moyen), version checker (7 flags). Rapport HTML produit : reviews/validation-report.html
- (event) Update run started — applying adversarial fixes S-01→S-06 from review-adversarial.md to architecture.md
- (decision) S-01 fix — PRNG canonique ajouté à D-02 : column seed = Long.hashCode(datasetSeed) ^ columnIndex ; new java.util.Random(columnSeed) — aucun autre générateur autorisé. Prévient divergence builder A/B sur reproductibilité NFR-R03.
- (decision) S-02 fix — Single writer status ajouté à D-08 : seul GenerationJobListener écrit generation_jobs.status après soumission ; dataset-api appelle requestCancel() uniquement. Prévient race condition sur statut terminal.
- (decision) S-03 fix — DDL URL prefix ajouté à D-05 : /api/workspaces/{wid}/projects/{pid}/subprojects/{spid}/ddl/{did}/** — interdit /api/ddl/**. Garantit couverture SpaceSecurityFilter sur endpoints DDL.
- (decision) S-04 fix — Export URL ajouté à D-05 : /api/workspaces/{wid}/datasets/{did}/export — membership vérifié avant streaming. Prévient cross-space data exfiltration via UUID direct.
- (decision) S-05 fix — Comportement API-Version absent ajouté à D-07 : header manquant → 400 BAD_REQUEST MISSING_API_VERSION — jamais de fallback silencieux. Prévient contrat ambigu entre implémentations API interceptor.
- (decision) S-06 fix — P-09 règle 4 et D-06 alignés : @Auditable couvre génération ET export. Prévient gap audit trail sur les exports non loggés (NFR-S04 100%).
- (decision) S-07 fix — React Query key taxonomy ajouté à D-08 : formes canoniques ['workspaces',wid], ['datasets',wid,pid,spid], ['dataset',did], ['generation',did]. Sur COMPLETED invalider ['dataset',did] + ['datasets',wid,pid,spid]. Prévient état stale liste datasets après génération.
- (event) Update terminé — 7 décisions amendées dans architecture.md (S-01→S-07). Document prêt.
